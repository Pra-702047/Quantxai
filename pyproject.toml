[project]
name = "quantx"
version = "1.0.0"
description = "QuantX AI Trading Platform"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "pandas",
    "numpy",
    "yfinance",
    "scikit-learn"
]

[tool.vercel]
entrypoint = "api:app"
